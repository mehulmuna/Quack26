{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:toxiproxy": "docker compose up -d",
    "start": "node src/index.js",
    "chaos:ensure": "node -e \"require('dotenv').config(); require('./src/chaos/ensureRuntime').ensureChaosRuntime().then(r=>console.log(JSON.stringify(r,null,2))).catch(e=>{console.error(e.message||e);process.exit(1)})\"",
    "test": "node src/test/attack-loop-smoke.js --tools-only",
    "gemini:demo": "node src/gemini/gemini_demo.js",
    "test:loop": "node src/test/attack-loop-smoke.js",
    "test:analyzer": "node src/test/codebase-analyzer-smoke.js",
    "test:analyzer:tools": "node src/test/codebase-analyzer-smoke.js --tools-only",
    "test:attack": "node src/test/attack-loop-smoke.js",
    "test:attack:tools": "node src/test/attack-loop-smoke.js --tools-only"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/genai": "^2.7.0",
    "dockerode": "^5.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "googleapis": "^173.0.0",
    "zod": "^4.4.3"
  }
}
